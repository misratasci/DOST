# Plot DOST Embedding (UMAP)

Projects the DOST embedding into 2D using UMAP.

## Usage

``` r
plot_DOST_umap(Z, labels)
```

## Arguments

- Z:

  The embedding matrix from DOST.

- labels:

  A vector of cluster labels.
